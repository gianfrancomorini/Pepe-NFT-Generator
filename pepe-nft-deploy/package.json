{
  "name": "pepe-nft-generator",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": "20.x",
    "npm": "10.9.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build && mv build ../",
    "test": "react-scripts test",
    "install-client": "cd build && npm install",
    "install-server": "npm install"
  },
  "dependencies": {
    "@pinata/sdk": "^2.1.0",
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}